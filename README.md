# Запуск мигратора:

* Если нужно мигрировать одну структуру, то следует указать её имя,
при запуске main файла. Пример:

`migrate User`

* Если нужно мигрировать все структуры, то нужно указать:

`migrate *`

